<Window x:Class="StudentInformationEntry.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentInformationEntry"
        mc:Ignorable="d"
        Title="StudentInformation Entry" Height="600" Width="1100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Navigation buttons -->
        <StackPanel Orientation="Horizontal" Margin="5">
            <Button Content="HomePage" Click="NavigateToHomePage_Click" Margin="8,0,8,0"/>
            <Button Content="StudentInformation" Click="NavigateToStudentInformation_Click" Margin="8,0,8,0"/>
            <Button Content="About Develelopers" Click="NavigateToAboutDevelopers_Click" Margin="8,0,8,0"/>
        </StackPanel>

        <!-- Frame for displaying pages -->
        <Frame Name="mainFrame" Grid.Row="1"/>
    </Grid>
</Window>
